/* 1. create table called SONG.
   2. create two columns for "title" and "artist".
   3. give the columns the datatype of varchar(100), meaning a string of 100 characters.
*/

CREATE TABLE song (
    title varchar(100) not null,
    artist varchar(100) not null
);  